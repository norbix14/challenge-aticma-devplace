/**
 * Sumar 1 a cada número en una lista de enteros.
 * 
 * @param {array} lista - listado de números.
 * @returns un nuevo array.
*/
function ejerciciodos(lista = [1, 2, 3, 4, 5, 7]) {
  return lista.map(val => val + 1)
}
